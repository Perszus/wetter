# Wetter ships no reflection-driven code yet, so the defaults in
# proguard-android-optimize.txt are sufficient. Add rules here as libraries that
# need them arrive (kotlinx.serialization models in particular), and say why.
