# Gradle
.gradle/
build/
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
captures/
.cxx/

# Signing material never belongs in the repository
keystore.properties
*.jks
*.keystore

# JVM crash dumps. Gradle drops these in whatever directory it died in, and
# they are tens of thousands of lines of machine-local paths and heap state.
hs_err_pid*.log
replay_pid*.log

# Working briefs and notes, kept locally. Their content lives in docs/ in a form meant to
# be read rather than followed. Root-anchored so docs/providers.md is tracked.
/philos.md
/providers.md
/upgrade.md
/notes.md

# Source artwork dropped in for conversion; the drawable is what ships.
/*.png

# OS noise
.DS_Store
Thumbs.db
