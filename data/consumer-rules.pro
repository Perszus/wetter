# Rules that every consumer of :data inherits. Empty for now: nothing in this
# module relies on reflection. Serializable response types are internal and are
# handled by the kotlinx.serialization plugin's own generated rules.
